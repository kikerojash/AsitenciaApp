package com.systemvv.grupo.asitenciaapp.asistenciaM.adapter.holder;



import butterknife.BindView;
import butterknife.ButterKnife;

public class AsistenciaFilasHolder {

      /*  extends FilasViewHolder<TipoAsistencia> {
    public static final String TAG = AsistenciaFilasHolder.class.getSimpleName();

    @BindView(R.id.txtTitle)
    TextView textViewTitulo;
    @BindView(R.id.root)
    ConstraintLayout fondo;

    public AsistenciaFilasHolder(View itemView) {
        super(itemView);
        ButterKnife.bind(this, itemView);
    }

    @Override
    public void bind(TipoAsistencia cell) {
        switch (cell.getMotivoAsistencia()) {
            case TIPO_ASISTENCIA_PRESENTE:
                fondo.setBackgroundColor(itemView.getResources().getColor(R.color.md_green_600));
                textViewTitulo.setText("Presente");
                Log.d(TAG, "TIPO_ASISTENCIA_PRESENTE");
                break;
            case TTIPO_ASISTENCIA_TARDE:
                Log.d(TAG, "TTIPO_ASISTENCIA_TARDE");
                textViewTitulo.setText("Tardanza");
                fondo.setBackgroundColor(itemView.getResources().getColor(R.color.md_yellow_900));
                break;
            case TIPO_ASISTENCIA_FALTO:
                Log.d(TAG, "TIPO_ASISTENCIA_FALTO");
                textViewTitulo.setText("Ausente");
                fondo.setBackgroundColor(itemView.getResources().getColor(R.color.md_red_A400));
                break;
        }

        // textViewTitulo.setText(cell.getMotivoAsistencia().name()+"");
    }*/
}